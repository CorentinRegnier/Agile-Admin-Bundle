{% extends 'knp_menu_base.html.twig' %}

{% block root %}
    <ol class="breadcrumb">
        <li><a href="{{ path('admin_homepage') }}"><i class="fa fa-home"></i> {{ 'admin.nav.homepage'|trans }}</a></li>
        {% for item in item.children %}
            {% if not loop.last %}
                <li>
                    <a href="{{ item.uri }}">
                        <i class="{{ item.attribute('icon') }}"></i>
                        {{ item.name|trans }}
                    </a>
                </li>
            {% else %}
                <li class="active">
                    <span>
                        <i class="{{ item.attribute('icon') }}"></i>
                        {{ item.name|trans }}
                    </span>
                </li>
            {% endif %}
        {% endfor %}
    </ol>
{% endblock %}
