{% extends 'AgileKernelBundle:form:theme.html.twig' %}

{% block form_errors -%}
    {% if errors|length > 0 -%}
        <div class="alert alert-danger alert-dismissible">
            <button type="button" class="close" data-dismiss="alert">×</button>
            {%- for error in errors -%}
                <span><i class="icon fa fa-ban"></i> {{ error.message }}!</span>
            {%- endfor -%}
        </div>
    {%- endif %}
{%- endblock form_errors %}
